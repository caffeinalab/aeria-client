import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import DatePickerRange from './index.js';
import { action } from '@storybook/addon-actions';
import { text } from '@storybook/addon-knobs';

<Meta title="DatePickerRange" component={DatePickerRange} />

# DatePickerRange

Use `DatePickerRange` component when you need to add some text.
<Preview>
  <Story name="DatePickerRange">
    <DatePickerRange
      id="name"
      name="name"
      type="text"
      label={text('DatePickerRange Label', 'Date Picker')}
    />
  </Story>
</Preview>

<br /><br />

#### Props definition

<Props of={DatePickerRange} />
