import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FieldGroup from './index.js';
import { action } from '@storybook/addon-actions';
import { text, boolean } from '@storybook/addon-knobs';

<Meta title="core/FieldGroup" component={FieldGroup} />


# FieldGroup

Use `FieldGroup` component when you need a multiple selection of values.

<Story name="FieldGroup">
    <FieldGroup
      id="feature"
      label={text('Label FieldGroup', 'Enable feature')}
      description={text('Description FieldGroup', 'Enable feature')}
      fields={[{
        type: "text",
        id: "input-text",
        label: "Name",
        description: "description",
        size: "half",
        placeholder: "placeholder",
        required: true
      },{
        type: "text",
        id: "input-text",
        label: "Surname",
        description: "description",
        size: "half",
        placeholder: "placeholder",
        required: true
      }]}
      onChange={action('onChange')}
    />
</Story>

<br /><br />

#### Props definition

<Props of={FieldGroup} />

