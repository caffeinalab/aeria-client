import styled from 'styled-components'
import {rem} from 'polished'

const StyledWrapperInfo = styled.div`
  width: 100%;
  display: flex;

  flex-direction: column;

  padding: ${rem('10px')} ${rem('20px')};
`

export default StyledWrapperInfo
