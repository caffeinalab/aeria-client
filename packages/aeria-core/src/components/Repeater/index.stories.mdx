import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Repeater from './index.js';
import { action } from '@storybook/addon-actions';
import { text, boolean } from '@storybook/addon-knobs';

<Meta title="core/Repeater" component={Repeater} />


# Repeater

Use `Repeater` component when you need a multiple selection of values.

<Story name="Repeater">
    <Repeater
      id="feature"
      label={text('Label Repeater', 'Enable feature')}
      description={text('Description Repeater', 'Enable feature')}
      min={3}
      max={5}
      fields={[{
        type: "text",
        id: "input-text",
        label: "Name",
        description: "description",
        size: "half",
        placeholder: "placeholder",
        required: true
      },{
        type: "text",
        id: "input-text",
        label: "Surname",
        description: "description",
        size: "half",
        placeholder: "placeholder",
        required: true
      }]}
      index={0}
      onChange={action('onChange')}
    />
</Story>

<br /><br />

#### Props definition

<Props of={Repeater} />

